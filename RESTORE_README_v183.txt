/restore
Then send ONE file: .json / .ison / .sqlite3.gz / .gz / .csv.
For chat backup JSON, payload chat_id must match the current chat.
Before restore the bot preserves pre_restore where supported.
