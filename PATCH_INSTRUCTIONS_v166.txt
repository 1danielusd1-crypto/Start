bot_v166_fast_parallel_forward_pairs

Рекомендуемый способ:
1. Распаковать bot_v166_FULL_PACKAGE_FLAT.zip в корень проекта с заменой файлов.
2. Не смешивать модули из разных версий.
3. Deploy на Render.

Changed-only:
bot_v166_CHANGED_FILES_ONLY.zip ставится ТОЛЬКО поверх полного v165_owner_first_circle_compat.
Он содержит:
- bot.py
- modules_manifest.json
- 116_v166_fast_parallel_forward_pairs.py

После deploy проверить:
1. /start.
2. INFO -> «Пересылка: по-новому/по-старому» несколько раз.
3. Пересылка: существующие пары должны появиться сверху в новом режиме и в статусе старого режима.
4. Изменить направление одной пары и сразу нажать кнопки в другом открытом окне — второе окно не должно ждать настройку первой пары.
5. Внести финансовое значение и проверить, что открытые Ф91 быстро получили новый баланс.
6. Если открыто два Ф91 одного чата, оба должны обновиться; одно окно не должно превращать/ломать другое.
