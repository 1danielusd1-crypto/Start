# v194 EXCEL CANONICAL OPENING TOTAL FINAL

- Один канонический расчет «Остаток с прошлого раза» для ARS/USD во всех Excel/Google/табличных путях.
- Остаток = сумма канонических движений валюты строго до начала периода; точный диапазон учитывает более ранние записи стартового дня.
- Месячные CSV/XLSX и /tabl_lsx переведены с raw amount/daily_records на канонический ARS-поток; USD остаётся отдельным.
- Старые CSV-команды и /csv больше не обходят современный канонический экспорт.
- Экран «Остаток после каждого расхода» использует тот же валютный поток.
- /tabl_lsx учитывает ручной category_override_slug; расчет еды использует общий v151 периодный расчет.
- JSON backup не переписывается и остается lossless restore-артефактом.
- Сохранены v193 formula-domain guard и единый Telegram/Google workbook builder.
