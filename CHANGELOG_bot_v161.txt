bot_v161_annotation_tenant_chat_sync

Исправлено:
- /iz-mr и /tz перехватываются до финансового парсера и пересылки, включая Telegram-вставку вида @bot /tz ...
- /iz-mr сохраняет имя и применяет его к конкретному видимому окну; имя повторно показывается при дальнейших перерисовках того же window-token.
- успешные /iz-mr и /tz явно фиксируются событиями window_annotation_saved / window_tz_saved в журнале.
- выгрузки маркировок и ТЗ используют то же window_annotations_v160 хранилище.
- Фин режим, Быстрый баланс, Скрытый финрежим и меню Пересылки получают список чатов из одного канонического первого круга tenant.chat_ids + root_chat_id.
- чаты с bot_removed больше не скрываются из списков первого круга; отображаются с существующей отметкой ➖.
- вместо глобального OWNER_ID для списков используется root_chat_id текущего пространства.
- восстановление полного state-export принимает bot_v161_*.

Не изменялась бизнес-логика финансовых записей, пересылки сообщений, Google/MEGA или напоминалок за пределами описанных маршрутов/UI.
